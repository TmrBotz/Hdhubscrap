services:
  - type: web
    name: hdhub4u-scraper
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python scraper.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
